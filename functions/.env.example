# FMZR backend environment variables
# -------------------------------------------------
# Copy this file to `.env` when running the Firebase Functions emulator.
# These values are available via process.env when using `dotenv` locally.

RP_ID=localhost
ORIGIN=http://localhost:5173

# For production secrets, prefer `firebase functions:config:set` or
# the Firebase console's runtime config rather than committing real keys.
